UPSTREAM_URL=https://mcp.deepwiki.com/mcp
# 如果未设置，上游 SSE 将由 UPSTREAM_URL 推断（/mcp -> /sse）
# UPSTREAM_SSE_URL=https://mcp.deepwiki.com/sse

# 需要的上游头（JSON 字符串），例如：
# {"Authorization":"Bearer <API_KEY>","Mcp-Session-Id":"<固定会话>"}
UPSTREAM_HEADERS_JSON={}

# 允许跨域的 Origin（默认 *）
CORS_ORIGIN=*

# 上游请求超时（毫秒），0 表示不施加硬超时
UPSTREAM_TIMEOUT_MS=0
